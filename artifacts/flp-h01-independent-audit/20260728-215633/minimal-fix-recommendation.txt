TASK: ET-FLP-H01-INDEPENDENT-ROOT-CAUSE-AUDIT
MODE: RECOMMENDATION ONLY; NO PATCH APPLIED
CLASSIFICATION: TEST_FIXTURE_CONFIRMED

MINIMAL FIX FOR A SEPARATE AUTHORIZED TASK
==========================================

Target file:
backend/tests/ExamTransfer.Infrastructure.Tests/SixFindingsV133BackendTests.cs

Remove/replace:
- The wait at line 77 for Volatile.Read(ref cloud.PushCalls) == 1.

Preferred deterministic gate:
1. Start the worker.
2. Poll the specific sync_queue row where:
   - EntityType == "exam_sessions"
   - EntityId == detail.Summary.Id.ToString()
   - Status == SyncStatus.Synced
3. Read GetProjectionReadinessAsync from a fresh context.
4. Assert:
   - Ready == true
   - Status == SyncStatus.Synced
   - Code == "PUBLICCLOUD_PROJECTION_READY"
   - exactly one logical exam_sessions queue row exists for the session
   - RetryCount == 0 for the successful no-retry case

Acceptable alternative:
- Poll GetProjectionReadinessAsync until Ready == true, then separately assert exactly one matching exam_sessions outbox row and one successful attempt.

Explicitly reject:
- PushCalls >= 1: the first successful call is SessionCreated audit, not the session projection.
- PushCalls == 3: it hard-codes unrelated audit count and remains coupled to transport details.
- Longer delay or timeout: it does not repair the invalid assertion semantics.
- Disabled/removed assertions for logical session uniqueness.
- Production code changes: no scoped production defect was found.

TESTS TO ADD OR ADJUST IN THE PATCH TASK
=========================================

1. Exact exam_sessions projection completed
   CreateAndOpen, run worker, wait for the matching session row to be Synced, assert readiness.

2. Audit plus session enqueue contract
   Assert two audit rows and one session row can coexist, while ShareReady depends only on the session row.

3. No duplicate logical session projection
   Assert Count(EntityType=exam_sessions, EntityId=sessionId) == 1 and one LastAttemptAtUtc in the success path.

4. Multiple wake signals are idempotent
   Pulse repeatedly, run one worker, assert the session projection is pushed once.

5. Batch processing ignores transient aggregate counters
   Use a batch with audit and session items and gate on persisted per-entity status.

6. Valid retry preserves readiness semantics
   First attempt fails, retry succeeds; Ready remains false until the matching session item is Synced.

7. Deterministic fixture
   No polling of exact transient PushCalls values; no arbitrary sleep or timeout increase.

EXPECTED PATCH SCOPE
====================

- Production files: 0
- Test files: 1
- Schema/migrations/RPC/RLS: 0
- Supabase remote operations: 0
- Installer/configuration: 0

RECOMMENDED VALIDATION ORDER
============================

1. Run the adjusted failing test 10 times sequentially.
2. Run all SixFindingsV133BackendTests.
3. Run the related backend module/project tests.
4. Build the backend solution.
5. Run broader regression only after the targeted gate is stable.

STOP CONDITION
==============

This audit does not authorize applying the recommendation. Wait for independent review and a separate patch task.

