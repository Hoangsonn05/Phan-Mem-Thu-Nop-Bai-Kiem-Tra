TASK: ET-FLP-H01-INDEPENDENT-ROOT-CAUSE-AUDIT
RESULT: TEST_FIXTURE_CONFIRMED
MODEL: Codex based on GPT-5 (current session)
REASONING: R3 / maximum available in current session
GIT HEAD: 219af954acc93dbeea11207673d5f5743c2ee5ab
BRANCH: master
SOURCE MODIFIED: NO

FAILING TEST:
SixFindingsV133BackendTests.PublicCreate_WakesWorkerAndBecomesExplicitlyShareReady

ASSERTION:
WaitUntilAsync(() => Volatile.Read(ref cloud.PushCalls) == 1), test line 77;
eventual Assert.True(condition), helper line 199.

EXPECTED:
True; the polling loop must sample the exact transient aggregate counter value 1.

ACTUAL:
Intermittent false. In the targeted-class reproduction:
Assert.True() Failure; Expected True; Actual False at lines 199 and 77.
Final runtime evidence was PushCalls=3, not 1.

PUSHCALLS DEFINITION:
Public Int32 field on private SuccessfulPushCloud, test line 231.

PUSHCALLS INCREMENT SITES:
One site: Interlocked.Increment(ref PushCalls) at test line 246, at entry to every fake PushAsync call.

PUSHCALLS SEMANTICS:
Aggregate attempted pushes across every eligible outbox item. It is not a session-only counter and not a ShareReady signal. Retries would increment it again.

THREAD SAFETY:
YES for this fixture: Interlocked increment plus Volatile read.

OUTBOX ITEMS CREATED:
3.

AUDIT ITEM:
2 distinct items:
1. SessionCreated
2. SessionStateChanged

SESSION ITEM:
1 exam_sessions/upsert item for the created session.

DUPLICATE SESSION ITEM:
NO. Source enqueues once in CreateAndOpenAsync and runtime observed exactly one session row in 10/10 runs.

WORKER START COUNT:
1 in the fixture. Production registers one hosted CloudSyncWorker and guards LocalServer with a single-instance mutex.

WAKE SIGNAL COUNT:
2 Pulse call sites execute for this create:
1. OutboxService after saving session outbox, before transaction commit.
2. SessionService after transaction commit.
Effective fixture wake count: 1, because the worker starts after both pulses and the bounded channel capacity is 1.

CLAIM COUNT:
3 per-item Syncing/lease transitions in one batch.

BATCH SIZE:
Configured maximum 10; actual eligible batch 3.

RETRY COUNT:
0 for every observed row and call.

PUSH CALL TIMELINE:
CALL 1:
audit_logs / SessionCreated / one successful attempt.

CALL 2:
audit_logs / SessionStateChanged / one successful attempt.

CALL 3:
exam_sessions / created session / one successful attempt.

ADDITIONAL CALLS:
None.

SESSION PROJECTION RESULT:
Synced in 10/10 monitored sequential runs; one logical row and one attempt.

AUDIT PROJECTION RESULT:
Both distinct audit rows Synced in 10/10 monitored sequential runs.

SHARE READY RESULT:
true in persisted state in 10/10 monitored sequential runs.

SHARE READY CONTRACT:
Computed dynamically by GetProjectionReadinessAsync from only the latest matching exam_sessions SyncQueueItem. Synced maps to Ready=true and PUBLICCLOUD_PROJECTION_READY. Audit rows and PushCalls do not participate.

REPEAT RUNS:
10 sequential monitored runs, plus initial reproduction, exact timeline capture, and targeted class.

PASS COUNT:
9/10 sequential exact-test runs.

FAIL COUNT:
1/10 sequential exact-test runs.

PARALLELIZATION EFFECT:
Not causal. Every exact-test invocation matched one test only and used unique local state. Configuration was not modified to force a separate runner mode because configuration is locked.

CONFIRMED ROOT CAUSE:
The test waits for an exact, transient, aggregate transport counter value. CreateAndOpen legitimately produces two audit outbox items and one session outbox item. The single worker processes all three sequentially and rapidly, so polling may sample 1 or may only sample final 3. The session projection itself succeeds exactly once and becomes ShareReady.

WHY PREVIOUS FIX DID NOT CLOSE GATE:
The prior SQLite fixes allowed the worker query and ordering to execute correctly. Once the worker can process the real batch, the remaining test still assumes the whole flow produces exactly one PushAsync call. That assumption does not match the outbox contract and is timing-sensitive.

PRODUCTION BUG: NO
Scope qualifier: no production defect was found in the FLP-H01 create/wake/process/ShareReady path or the supported single-LocalServer topology. This audit does not claim remote Supabase or arbitrary unsupported multi-host runtime validation.

TEST/FIXTURE BUG: YES

MINIMAL PRODUCTION FIX:
None.

MINIMAL TEST FIX:
In a separate authorized patch task, remove the exact aggregate PushCalls == 1 wait. Wait for the specific exam_sessions outbox item to become Synced, or poll GetProjectionReadinessAsync until Ready=true. Then assert exactly one logical exam_sessions queue row/attempt. Do not replace the assertion with PushCalls >= 1 because an audit push can occur first.

FILES LIKELY AFFECTED:
backend/tests/ExamTransfer.Infrastructure.Tests/SixFindingsV133BackendTests.cs only.

REGRESSION RISKS:
- A weak replacement such as PushCalls >= 1 can pass before the session projection completes.
- Removing all push assertions can miss duplicate session projection.
- Depending only on aggregate queue completion would incorrectly couple ShareReady to audit delivery.
- Adding delay/timeout would preserve the invalid transient assertion.

REQUIRED TESTS:
1. Exact exam_sessions projection completed.
2. Audit + session enqueue together while readiness follows only the defined session contract.
3. No duplicate logical session projection.
4. Multiple wake signals remain idempotent.
5. Two/three-item batch does not depend on a transient aggregate counter.
6. Valid retry does not compute ShareReady incorrectly.
7. Deterministic wait on persisted business state, not exact transient PushCalls.

SUPABASE UPDATE REQUIRED: NO
MIGRATION REQUIRED: NO
REMOTE COMMANDS RUN: NO
SOURCE FILES CHANGED: 0

ARTIFACTS:
artifacts/flp-h01-independent-audit/20260728-215633/source-trace.txt
artifacts/flp-h01-independent-audit/20260728-215633/push-call-timeline.txt
artifacts/flp-h01-independent-audit/20260728-215633/repeat-run-results.txt
artifacts/flp-h01-independent-audit/20260728-215633/root-cause-report.txt
artifacts/flp-h01-independent-audit/20260728-215633/minimal-fix-recommendation.txt

NEXT STEP: WAIT_FOR_REVIEW

