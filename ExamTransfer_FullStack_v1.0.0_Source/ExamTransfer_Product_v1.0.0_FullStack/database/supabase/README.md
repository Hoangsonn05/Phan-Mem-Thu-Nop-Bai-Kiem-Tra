# Legacy Supabase path

This directory is intentionally documentation-only. The sole authoritative Supabase project is [`backend/supabase`](../../backend/supabase/README.md), including `config.toml`, migrations, seed data, functions, and pgTAP tests.

Do not add migrations here and do not run `supabase` commands from this directory. Historical files remain available through Git history.
